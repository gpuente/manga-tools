import { createStyles } from '@utils';

export default createStyles({
  actionArea: {
    display: 'flex',
    flexDirection: 'row',
  },
  cardMedia: {
    maxWidth: 130,
    height: 200,
  },
  card: {
    width: 400,
    height: 200,
  },
});
